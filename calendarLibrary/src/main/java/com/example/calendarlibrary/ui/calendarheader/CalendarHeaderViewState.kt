package com.example.calendarlibrary.ui.calendarheader

/**
 * Represents the view state for the calendar header, which typically displays
 * the current date or month and year.
 *
 * @property currentDate The string representation of the current date, month,
 *                       and year to be shown in the header (e.g., "September 2024").
 */
data class CalendarHeaderViewState(
    val currentDate: String
)
